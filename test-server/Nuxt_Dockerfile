# build stage
FROM node:24 AS build
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# production stage
FROM node:24
WORKDIR /app
COPY --from=build /app/.output ./
ENV NITRO_PORT=3000
ENV NITRO_HOST=0.0.0.0
EXPOSE 3000
CMD ["node", "server/index.mjs"]
