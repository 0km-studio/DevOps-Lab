# this docker-compose for gitlab vm and test server 🔥

services:
  gitlab:
    image: gitlab/gitlab-ce
    container_name: gitlab
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url "http://192.168.1.39"
        gitlab_rails['initial_root_password'] = "wearesame"
        gitlab_rails['registry_enabled'] = true
        registry_external_url "http://192.168.1.39:5001"
        nginx["listen_port"] = 80
        nginx["listen_https"] = false 
        nginx["https"] = false
    ports:
      - "8080:80"
      - "5001:5001"
    volumes:
      - ./gitlab-config:/etc/gitlab
      - ./gitlab-logs:/var/log/gitlab
      - ./gitlab-data:/var/opt/gitlab
    networks:
      - nn
  
  gitlab-runner:
    image: gitlab/gitlab-runner
    container_name: gitlab-runner
    privileged: true
    volumes:
      - ./gitlab-runner:/etc/gitlab-runner
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - nn

  mongo:
    image: mongo
    container_name: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: wearesame
      MONGO_INITDB_ROOT_PASSWORD: eiei
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
    networks:
        - nn

volumes:
  mongodata:

networks:
  nn:
    driver: bridge
